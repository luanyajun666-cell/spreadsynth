version: "3.9"

services:
  backend:
    build: ./backend
    container_name: spreadsynth-backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: spreadsynth-frontend
    depends_on:
      - backend
    ports:
      - "8501:8501"
